import { Organisation } from '../authentication/organisation.interface';

export const mockOrganisation: Organisation = {
  domainName: 'test',
  name: 'test',
};

export const mockOrganisations: Organisation[] = [mockOrganisation];
